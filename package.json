{
  "name": "smart-patient-queue-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-backend": "cd backend && uvicorn app.main:app --reload",
    "start-frontend": "cd frontend && npm start",
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}